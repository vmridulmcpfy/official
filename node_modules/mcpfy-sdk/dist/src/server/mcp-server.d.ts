import { McpServer as OfficialMcpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { type ToolCallback, type ToolDefinition } from "./tools.js";
import { type PromptCallback, type PromptDefinition } from "./prompts.js";
import { type ReadResourceCallback, type ReadResourceTemplateCallback, type ResourceDefinition, type FlatResourceTemplateDefinition } from "./resources.js";
import { type UIResourceDefinition, type WidgetCallback } from "./widgets/index.js";
export interface MCPServerConfig {
    name: string;
    version: string;
    description?: string;
}
export interface ListenOptions {
    /** Defaults to "stdio" — the transport most MCP hosts (Claude Desktop, Claude Code, etc.) use to launch servers. */
    transport?: "stdio" | "http";
    /** HTTP only. Defaults to `process.env.PORT` or 3000. */
    port?: number;
    /** HTTP only. Defaults to "localhost". */
    host?: string;
}
export declare class MCPServer {
    /** The underlying `@modelcontextprotocol/sdk` McpServer instance, for advanced/escape-hatch use. */
    readonly nativeServer: OfficialMcpServer;
    readonly config: MCPServerConfig;
    private httpHandle?;
    constructor(config: MCPServerConfig);
    tool<TInput = Record<string, any>, TOutput extends Record<string, unknown> = Record<string, unknown>>(def: ToolDefinition<TInput, TOutput>, cb?: ToolCallback<TInput, TOutput>): this;
    prompt<TInput = Record<string, any>>(def: PromptDefinition<TInput>, cb?: PromptCallback<TInput>): this;
    resource(def: ResourceDefinition, cb?: ReadResourceCallback): this;
    resourceTemplate<TParams extends Record<string, any> = Record<string, any>>(def: FlatResourceTemplateDefinition<TParams>, cb?: ReadResourceTemplateCallback<TParams>): this;
    widget<TInput = Record<string, any>>(def: UIResourceDefinition<TInput>, cb?: WidgetCallback<TInput>): this;
    listen(options?: ListenOptions): Promise<void>;
    close(): Promise<void>;
}
//# sourceMappingURL=mcp-server.d.ts.map
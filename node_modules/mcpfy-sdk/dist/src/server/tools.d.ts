import type { McpServer as OfficialMcpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { ToolAnnotations } from "@modelcontextprotocol/sdk/types.js";
import type { z } from "zod";
import type { ToolContentResult, TypedCallToolResult } from "../shared/response-helpers.js";
import { type ToolContext } from "./context.js";
export type ToolCallback<TInput = Record<string, any>, TOutput extends Record<string, unknown> = Record<string, unknown>> = (params: TInput, ctx: ToolContext) => Promise<TypedCallToolResult<TOutput> | ToolContentResult>;
export interface ToolDefinition<TInput = Record<string, any>, TOutput extends Record<string, unknown> = Record<string, unknown>> {
    name: string;
    title?: string;
    description?: string;
    /** A `z.object({...})` schema describing the tool's input parameters. */
    schema?: z.ZodTypeAny;
    /** A `z.object({...})` schema constraining `structuredContent` in the tool's result. */
    outputSchema?: z.ZodTypeAny;
    annotations?: ToolAnnotations;
    cb?: ToolCallback<TInput, TOutput>;
}
export declare function registerTool<TInput = Record<string, any>, TOutput extends Record<string, unknown> = Record<string, unknown>>(nativeServer: OfficialMcpServer, def: ToolDefinition<TInput, TOutput>, cb?: ToolCallback<TInput, TOutput>): void;
//# sourceMappingURL=tools.d.ts.map
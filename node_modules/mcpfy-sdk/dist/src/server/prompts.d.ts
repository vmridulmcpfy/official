import type { McpServer as OfficialMcpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { GetPromptResult } from "@modelcontextprotocol/sdk/types.js";
import type { z } from "zod";
import type { ToolContentResult, TypedCallToolResult } from "../shared/response-helpers.js";
import { type ToolContext } from "./context.js";
export type PromptCallback<TInput = Record<string, any>> = (params: TInput, ctx: ToolContext) => Promise<GetPromptResult | TypedCallToolResult<any> | ToolContentResult>;
export interface PromptDefinition<TInput = Record<string, any>> {
    name: string;
    title?: string;
    description?: string;
    /** A `z.object({...})` schema describing the prompt's arguments. */
    schema?: z.ZodObject<any>;
    cb?: PromptCallback<TInput>;
}
export declare function registerPrompt<TInput = Record<string, any>>(nativeServer: OfficialMcpServer, def: PromptDefinition<TInput>, cb?: PromptCallback<TInput>): void;
//# sourceMappingURL=prompts.d.ts.map